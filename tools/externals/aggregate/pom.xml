<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>de.maggu2810.shk.tools.externals</groupId>
    <artifactId>pom</artifactId>
    <version>1.8-SNAPSHOT</version>
  </parent>

  <artifactId>aggregate</artifactId>
  <packaging>pom</packaging>

  <name>shk :: Tools :: Externals :: Aggregate</name>

  <dependencies>
    <!-- Aries -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-aries</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Commons -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-commons</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Eclipse SmartHome -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-esh</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Felix -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-felix</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- HK2 -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-hk2</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- JAX-RS -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-jaxrs</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Jersey -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-jersey</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Jetty -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-jetty</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Karaf -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-karaf</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- openHAB Core -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-ohc</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Others -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-others</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Pax Web -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-pax-web</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Spring -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-spring</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- XStream-->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-xstream</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>

    <!-- Xtext -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>ext-xtext</artifactId>
      <version>${project.version}</version>
      <type>pom</type>
    </dependency>
  </dependencies>

</project>
